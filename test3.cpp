#include <iostream>
#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    vector<int> v;
    v.push_back(4);
    v.push_back(5);
    v.push_back(3);
    v.push_back(1);
    v.push_back(2);

    for (auto i = v.begin(); i != v.end(); i++)
    {
        cout < < < < ", ";
    }
}